fastapi>=0.111
python-dotenv>=1.0       # load .env file in development
uvicorn[standard]>=0.29
httpx>=0.27
pyyaml>=6.0
aiofiles>=23.0        # required for FileResponse
psycopg2-binary>=2.9  # required for Postgres backend (optional if using SQLite only)
bcrypt>=4.0           # user password hashing
cryptography>=42.0    # AI key encryption at rest (Fernet)
stripe>=8.0           # Stripe billing integration
